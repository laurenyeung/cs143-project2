Name: Lauren Yeung
ID:104456482

Name: Michael Wang
ID:204458659
================================================================
Task 1: Implementing DiskPartition and GeneralDiskHashedRelation
Implemented DiskPartition#insert (inserts data)
            DiskPartition#closeInput(closes input)
            DiskPartition#getData (returns an iterator over its data)
            GeneralDiskHashedRelation#getIterator (returns an iterator over its DiskPartitions)
            GeneralDiskHashedRelation#closeAllPartitions (closes all DiskPartitions)
In addition DiskPartition#writtenToDisk is initialized to true for convenience
            DiskPartition#spillPartitionToDisk calls data.clear() after writing data

================================================================
Task 2: Implementing object DiskHashedRelation
Implemented DiskHashedRelation#apply (creates and returns a DiskHashedRelation from an Iterator[Row])

================================================================
Task 3: Implementing CS143Utils methods
Implemented CS143Utils#getUdfFromExpression (returns the last UDF (if any) from a Seq[Expression])
            Iterator[Row] methods (hasNext, next) in CachingIteratorGenerator#apply

================================================================
Task 4: Implementing PartitionProject
Implemented PartitionProject#generateIterator (methods: hasNext, next, fetchNextPartition)

================================================================
